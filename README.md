commandline interface with helper cmds for expressa applications 

## Usage

    $ npm install expressa-cli -g
    $ expressa
    Usage: 
      expressa json2collection <collectionname> <jsonfile>             <- converts json payload into data/collection/collectionname.json


